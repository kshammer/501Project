Compiled on Java 10. Anything above Java 8 should work. 
To Compile
javac graph.java 
To run
java graph < input.txt > output.txt  (this is for windows)
Input graph must not be a forest 
Input graph even with no predefined colors should have at least three 0's at the end of the file 